package DataAccess;

import Model.Orders;

/**
 * The class handles database operations for {@link Model.Orders} objects.
 * It extends {@link AbstractDAO<Orders>} to inherit common data access functionality.
 */
public class OrderDAO extends AbstractDAO<Orders> {}
